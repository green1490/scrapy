use std::collections::HashMap;
use super::interface::fetch::Fetch;
use bud::Bud;

pub mod bud;

// pub const scrapers:HashMap<String, Box<dyn Fetch>> = HashMap::from([
//     ("bud",Box::new(bud))
// ]);
